interface:
  display_name: "Draft a structured questionnaire"
  short_description: "Design and validate a small questionnaire with single-choice"
  default_prompt: "Use $research-questionnaire to complete this workflow with a reviewed plan and validated output."
policy:
  allow_implicit_invocation: true
